import 'package:flutter/material.dart';

AppBar appBar = AppBar(
    title: Text(
      'Home',
      style: TextStyle(color: Colors.white),
    ),
    actions: [
      IconButton(
        icon: const Icon(
          Icons.search,
        ),
        onPressed: () {},
      ),
    ]);
